<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuantumOS">

        <activity
            android:name="com.quantumos.shell.ui.LauncherActivity"
            android:exported="true"
            android:enableOnBackInvokedCallback="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- For the spike this is a normal tappable app. The HOME-launcher intent
                 (category.HOME) is deliberately NOT added yet — that arrives on hardware. -->
        </activity>
    </application>
</manifest>
